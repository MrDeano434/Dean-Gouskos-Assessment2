using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Interactions : MonoBehaviour {

	// Use this for initialization
	void Start ()
    {
		
	}
	
	// Update is called once per frame
	void Update ()
    {
		
	}
}

/* Need to define an array for weapon spaces so when they get picked up, they can be used
 * Need to define a reload system and button
 * Need to have a button for interacting with the environments buttons/collectables
 */
